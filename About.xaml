<Window x:Class="Android_Transfer_Protocol.About"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:prop="clr-namespace:Android_Transfer_Protocol.Properties"
        mc:Ignorable="d"
        Title="{x:Static prop:Resources.About}"
        WindowStartupLocation="CenterScreen"
        WindowStyle="ToolWindow"
        SizeToContent="WidthAndHeight">
    <DockPanel>
        <DockPanel DockPanel.Dock="Top">
            <Image Source="ATP.png" Width="70" Height="70"/>
            <Label Content="{x:Static prop:Resources.ATP}" FontSize="22" VerticalAlignment="Center"/>
        </DockPanel>
        <DockPanel DockPanel.Dock="Top">
            <Label Content="{x:Static prop:Resources.Version}" HorizontalAlignment="Center" Name="Version"/>
            <Label Content="{x:Static prop:Resources.Author}" HorizontalAlignment="Center" Name="Author"/>
        </DockPanel>
    </DockPanel>
</Window>
